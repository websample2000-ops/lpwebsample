movieLabel:
  title: "動画の区分け用ラベル"
  headerRow: 1
  dataStartRow: 2
  columns:
    id:
      type: number
      description: "管理用番号"
      required: true
      
    tagLabel:
      type: string
      description: "タグの名称"
      required: true

    priority:
      type: number
      description: "表示優先度"
      min: 1
      required: true